{
  "rhosts": {
    "types": "ips",
    "desc": "Host to inspect (IP), range (ie: '192.168.0.1-256', '192.168.0.0/24') or path of a file with them.",
    "default": "127.0.0.1"
  },
  "rports": {
    "types": "ports",
    "desc": "Port (or multiple sepatared with commas) to inspect, or range (22-8080) on chosen IPs",
    "default": "80,443,8008,8080"
  },
  "delay": {
    "types": "natural",
    "desc": "Delay between batches of requests (depending of the 'concurrency'), in ms).",
    "default": 0
  },
  "concurrency": {
    "types": "natural",
    "desc": "Number of requests to do at the same time. Only works if 'delay' is not 0",
    "default": 100
  },
  "timeout": {
    "types": "natural",
    "desc": "Time to wait for a response, in ms.",
    "default": 5000
  }
}
